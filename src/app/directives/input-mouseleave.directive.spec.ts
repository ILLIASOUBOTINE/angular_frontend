import { InputMouseleaveDirective } from './input-mouseleave.directive';

describe('InputMouseleaveDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMouseleaveDirective();
    expect(directive).toBeTruthy();
  });
});
