import { BtnNameAvailPipe } from './btn-name-avail.pipe';

describe('BtnNameAvailPipe', () => {
  it('create an instance', () => {
    const pipe = new BtnNameAvailPipe();
    expect(pipe).toBeTruthy();
  });
});
